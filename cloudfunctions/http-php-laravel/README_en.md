# http-php-laravel

A CloudBase HTTP function template powered by the [Laravel](https://laravel.com/) framework (skeleton).

Because a full Laravel project contains thousands of files, this template only ships the **entry conventions** — it does NOT pre-populate the entire scaffold. Initialize the project structure as described below and then add your custom routes/code.

## Recommended layout (after `composer create-project`)

```text
http-php-laravel/
├── scf_bootstrap       # Bootstrap: php -S 0.0.0.0:9000 -t public public/index.php
├── composer.json       # Provided
├── app/                # Laravel application code (HTTP / controllers / services)
├── bootstrap/
├── config/
├── public/
│   └── index.php       # Laravel entry (generated by the scaffold)
├── routes/
│   └── web.php / api.php
├── storage/
├── .env                # Environment variables (do NOT commit)
└── README_cn.md / README_en.md
```

## Initialize the scaffold

The recommended workflow is to scaffold a fresh Laravel project in a temp dir, then move the files into this template:

```bash
# 1. Create a fresh Laravel project in a temp directory
composer create-project laravel/laravel /tmp/laravel-app

# 2. Copy the generated files into this template (keep scf_bootstrap and README)
rsync -a /tmp/laravel-app/ ./ --exclude README.md

# 3. Generate the application key
php artisan key:generate

# 4. Install dependencies (if not already installed by the step above)
composer install
```

## Run locally

```bash
# Laravel's built-in dev server (recommended for local dev)
php artisan serve --host=0.0.0.0 --port=9000

# Or use PHP's built-in web server (matches scf_bootstrap)
php -S 0.0.0.0:9000 -t public public/index.php

# Then open http://localhost:9000
```

## Add sample routes

Edit `routes/web.php` (or `routes/api.php`):

```php
use Illuminate\Support\Facades\Route;

Route::get('/', function () {
    return response()->json([
        'message'   => 'Hello World from Laravel!',
        'timestamp' => now()->toIso8601String(),
    ]);
});

Route::post('/echo', function (\Illuminate\Http\Request $request) {
    return response()->json([
        'code'        => 200,
        'requestBody' => $request->all(),
    ]);
});
```

## Deploy to CloudBase HTTP Function

1. Run `composer install --no-dev --optimize-autoloader` locally to generate `vendor/`.
2. Upload the full folder (including `scf_bootstrap`, `composer.json`, `vendor/`, `public/`, `app/`, ...).
3. In the CloudBase console, create a new "HTTP-triggered function" with `PHP` or CustomRuntime as the runtime.
4. Configure production environment variables (`APP_KEY`, database, ...). Prefer console-injected env vars over uploading `.env` (especially `APP_KEY`).


<!-- tcb-cli-deploy-section -->
## 使用 tcb CLI 部署 / Deploy with tcb CLI

本目录提供了 `cloudbaserc.json`（`type: http`、`runtime: CustomRuntime`、`path: /http-php-laravel`）。本函数依赖 `scf_bootstrap` 启动自定义运行时进程，由 CloudBase CLI 一键打包上传。
> 在部署前请执行 `composer install --no-dev --optimize-autoloader`，确保 `vendor/` 已存在。

This folder ships a ready-to-use `cloudbaserc.json` (`type: http`, `runtime: CustomRuntime`, `path: /http-php-laravel`). It relies on `scf_bootstrap` to launch the custom runtime; CloudBase CLI packages and uploads everything in one go.
> Before deploying, run `composer install --no-dev --optimize-autoloader` so that `vendor/` exists.

```bash
# 1. Install & login
npm install -g @cloudbase/cli
tcb login

# 2. (按需) 先准备构建产物 / Optionally build artifacts first
#    Go:   ./build.sh
#    Java: mvn clean package && cp target/app.jar ./app.jar
#    PHP:  composer install --no-dev --optimize-autoloader

# 3. 部署 / Deploy
tcb fn deploy -e <YOUR_ENV_ID>

# 4. 访问 / Invoke via HTTP
curl https://<YOUR_ENV_ID>.service.tcloudbase.com/http-php-laravel/

# 5. 查看日志 / Tail logs
tcb fn log http-php-laravel -e <YOUR_ENV_ID>
```

> 一次性部署所有函数：在 `cloudfunctions/` 根目录执行 `tcb fn deploy --all -e <YOUR_ENV_ID>`。
> Deploy everything at once from the `cloudfunctions/` root: `tcb fn deploy --all -e <YOUR_ENV_ID>`.
