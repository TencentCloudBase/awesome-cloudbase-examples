# Node.js WebSocket CloudBase Run Template

A WebSocket real-time communication example built with Node.js + Express-WS, deployed via Docker to CloudBase Run.

## Quick Start

```bash
npm install
npm start
# WebSocket connect to ws://localhost:8080
```

## Deploy to CloudBase Run

```bash
tcb cloudrun deploy --yes -e <ENV_ID>
```
