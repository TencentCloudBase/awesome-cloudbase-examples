/**
 * 后端客户端初始化
 *
 * TODO: 安装并初始化你选择的后端客户端,导出 app 实例。
 *
 * 约束:
 * - 必须使用 npm 安装依赖,不能使用 CDN
 * - 导出的 app 实例将在 session.ts 和 todo-service.ts 中使用
 */

// TODO: 在这里初始化后端客户端并导出 app 实例
export const app: any = null;
