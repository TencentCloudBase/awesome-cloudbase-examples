/**
 * 后端 SDK 初始化
 *
 * TODO: 安装并初始化你选择的后端 SDK，导出 app 实例
 *
 * 示例（根据你使用的平台选择）：
 *
 *   // 方式一：
 *   import { createClient } from "@supabase/supabase-js";
 *   const app = createClient("YOUR_URL", "YOUR_ANON_KEY");
 *
 *   // 方式二：
 *   import cloudbase from "@cloudbase/js-sdk";
 *   const app = cloudbase.init({ env: "YOUR_ENV_ID" });
 *
 * 注意：
 * - 必须使用 npm 安装 SDK，不能使用 CDN
 * - 导出的 app 实例将在 auth.ts、cms-service.ts、storage-service.ts 中使用
 */

// TODO: 在这里初始化后端 SDK 并导出 app 实例
export const app: any = null;
