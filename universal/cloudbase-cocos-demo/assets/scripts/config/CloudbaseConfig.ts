/**
 * CloudBase 配置文件
 */

// 默认配置（可通过界面修改）
export const cloudbaseConfig = {
  env: '',
  region: 'ap-shanghai',
  accessKey: '',
};

// 本地存储键名
export const STORAGE_KEYS = {
  CONFIG: 'cloudbase_config',
  USER: 'cloudbase_user',
};
